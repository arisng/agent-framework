<Project>
  <Import Project="../../../../tests/Directory.Build.props" />

  <PropertyGroup>
    <!-- Keep the sample tests aligned with repo-standard test settings, but single-target net10.0. -->
    <TargetFrameworks>net10.0</TargetFrameworks>
  </PropertyGroup>
</Project>
